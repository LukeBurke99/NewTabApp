@using NewTabApp.Services
@inject HttpClient _client
@inject JsConsoleService _log
@page "/"




<PageTitle>Index</PageTitle>

<h1>Hello, world!</h1>


@code {

    protected override async Task OnInitializedAsync()
    {
        await GetBookmarksAsync();
    }

    private async Task GetBookmarksAsync()
    {
        const string path = @"C:\Users\lukej\AppData\Local\Google\Chrome\User Data\Default\Bookmarks";
        var bookmarks = await _client.GetStringAsync(path);
        await _log.LogToConsole(bookmarks);
    }

}